import styled from "styled-components";

export const Wrapper = styled.div`
   width: 100%;
   height: 100vh;
   /* overflow-x: scroll; */
   overflow-y: hidden;
   margin: 0px;
   padding: 0px;
   display: grid;
   grid-template-columns: 240px 1fr 0.75fr;
   grid-template-rows: 90px 1fr;
   background-color: #4a465f;

   /* background-color: #4a465f; */
`;
